# Handoff 2026-05-28

## Current Preview

- Use `npm run serve:dist`.
- Open `http://127.0.0.1:4180/`.
- Do not preview with `file://.../index.html`; data loading, asset paths, and `/nfc/*` fallback depend on the local server.

## Verified Commands

```sh
npm run build:fast
npm run check:dist
```

Latest result: `dist/ publish check passed (107 files)`.

## Main H5 Changes

- Netlify output is built into `dist/` via `scripts/build_dist.mjs`.
- Publish safety check is in `scripts/check_dist_publish.mjs`.
- Local `/nfc/*` preview fallback is in `scripts/serve_dist.mjs`.
- Bird nest page copy now uses:
  - title: `小鸟自留地`
  - eyebrow: `已入窝的鸟团团`
  - summary: `可收录的鸟团团会定期更新`
- Joyi Xiaohongshu product traffic entry points are on the home screen, nest summary card, and bird detail sheet.
- Clicking an unlocked nest bird card opens a screenshot-friendly bird sign detail sheet.
- Home daily bird card no longer plays bird audio after reveal.
- Bird audio remains available in nest cards and the detail sheet.

## Assets

- Bird call files live in `assets/bird-calls/`.
- Original bird call backups live in `assets/bird-calls-original/`.
- `npm run audio:optimize` trims project bird calls to about 8 seconds, mono, 96 kbps.
- Current optimized audio count: 34 mp3 files.
- Bird PNGs live in `assets/birds-final/`.
- WebP files live in `assets/birds-final-webp/`.
- `scripts/clean_bird_alpha_edges.py` removes the magenta edge pixels from bird PNG transparency.
- `npm run assets:webp` regenerates WebP assets from cleaned PNGs.

## Important Notes For Next Agent

- Do not deploy to Netlify until the user explicitly asks.
- Preserve test-only preview tools. They are hidden unless local preview mode is active.
- Keep future local validation on `http://127.0.0.1:4180/`.
- The worktree is intentionally dirty with many changed assets and generated files. Do not reset or revert unrelated files.
- Before release, run the full `npm run build` once, then `npm run check:dist`.
