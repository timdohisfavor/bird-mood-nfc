# 2026-05-28

## H5 UI 设计审查 + P0 修复

- 读完 handoff-2026-05-28.md，启动本地预览 `http://127.0.0.1:4180/`
- 系统性审查了 H5 前端 UI（HTML + CSS + JS），输出 8 项改进建议
- **P0 修复**（已完成并构建验证）：

### 字重层级（styles.css 源文件编辑）
- `.subcopy` → +font-weight: 500
- `.seal-copy` → +font-weight: 500
- `.quote` → 700→600
- `.home-note p, .guide-summary p` → +font-weight: 500
- `.tile-copy` → +font-weight: 500
- `.poster-quote` → 850→600
- `.detail-quote` → 850→600

### 鸟图放大
- `.bird-frame` height: 180px → 240px
- `.bird-frame img` width/height: 75% → 90%
- `.bird-sign.revealed` min-height: 500px → 560px
- 小屏适配同步调整: `.bird-sign.revealed` 492px → 540px

## 构建
- `npm run build:fast` 通过，dist/ 107 文件
- 源文件：根目录 `styles.css` → build 时复制到 `dist/styles.css`

## P1+P2 全量迭代

### P1-1: .look-line 去掉固定 min-height
- `min-height: 86px` → 移除，改为 `margin: 9px auto 8px`

### P1-2: 简化按钮/卡牌双触发
- 未翻开时隐藏 `#draw-button`（CSS: `.bird-sign.unrevealed ~ .primary-actions` opacity 0 + pointer-events none）
- 翻开后按钮淡入显示"已收录，查看图鉴"（延迟 180ms 过渡）

### P1-3: 鸟图加 drop-shadow
- `.bird-frame img` filter: none → `drop-shadow(0 8px 14px rgba(18,50,37,0.14))`

### P2-1: 小红书入口视觉权重
- `.brand-pill` font-size 12→13px, min-height 32→36px, padding +3px, border 微增

### P2-2: 统一卡片高度避免翻牌跳变
- `.bird-sign` min-height: 430→560px（与 revealed 一致）
- 小屏适配同步: 420→540px

### P2-3: Primary 按钮 disabled 态可读性
- `color: rgba(255,255,255,0.85)`, `background: rgba(47,111,69,0.45)`, `box-shadow: none`

## 深林背景 + 鸟蛋破壳 大改造

### 背景重写 (habitat-bg)
- `.sky`: 森林冠层渐变色 0d1f11→d8e6c8（8 stops）+ 光斑纹理
- `.canopy-rays`: 4 层对角光柱穿过树冠
- `.sun`: 暖色漫射 + sunBreathe 呼吸动画
- `.mist`: 2→3 层 + mistDrift 漂浮动画
- `.ridge`: 2→3 层纵深 (back/mid/front)
- `.forest-floor`: 替代 water — 落叶地表纹理
- `.trees`: 替代 forest — 树干+树冠剪影
- `.brush`: 新增灌木丛
- `.fireflies`: 6 颗萤火虫 + fireflyDrift
- body/html 背景色同步更新为森林色调

### 鸟蛋破壳 (sign-back)
- `.ambient-ring` → `.egg-glow`: 暖色巢穴光晕
- `.seal-mark` → `.bird-egg`: CSS 蛋形 88x118 + 5 层光泽 + 9 层斑点
- `.egg-crack-line`: 裂壳线 pseudo-elements
- 动画: eggHatch → crackReveal → glowBurst → nestTremble
- 文案: "帮小鸟破壳而出"
- meta theme-color: e8f1e9 → 1a2f1d

## 预览修复（最终方案）
- `script.js` 回退到 git HEAD，仅做一处改动：loadBirds 优先读 embeddedBirds
- `index.html`/`styles.css` 均回退到 git HEAD（丢失 P0-P2 CSS 改动，需后续重做）
- **必须用 HTTP 服务打开**：`npm run serve:dist` → `http://127.0.0.1:4180/`
- `file://` 协议天生不支持 fetch，无法绕过，不要尝试双击 HTML

## 森林摄影背景 + 磨砂玻璃主题迭代

### 背景切换
- 应用 Seedream 4.5 生成的真实森林照片 `forest-photo-seedream-v2.png`（晨光、丁达尔光、苔藓、晨露）
- 采用磨砂玻璃卡片风格 (backdrop-filter: blur(20px) + rgba(255,255,255,0.68))
- meta theme-color: #e8f1e9 → #1a2a1a
- commit: 630ca72

### 文字可见性修复（55899ce）
- **问题**: 深色森林照片背景 + 原浅色主题文字变量 = 标题/eyebrow 完全不可见
- **修复**:
  - h1 显式设置 `color: rgba(255,255,255,0.92)`（白色）
  - .eyebrow 改 `color: #a5c4a0`（亮绿，原来 var(--leaf)=#4a7c59 太暗）
  - 所有玻璃卡片内 `.draw-stage, .home-note, .bird-tile, .detail-sheet, .tabbar, .poster-card, .habitat-chip` 作用域覆盖 `--muted: #607366`（深灰），解决 `rgba(255,255,255,0.55)` 在白色磨砂背景上消失的问题
